apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
spec:
  selector:
    app: {{ .Release.Name }}
  ports:
  - port: {{ .Values.svcport }}
    targetPort: {{ .Values.tgport }}
    nodePort: 31081
  type: NodePort
